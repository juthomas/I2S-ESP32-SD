import { Data } from "../../App";


interface UploadFileProps {
  data?: Data;
  fetchData: () => Promise<void>;
}

export const UploadFile = ({ data, fetchData }: UploadFileProps): JSX.Element => {
  return (
    <>

    </>
  );
};
